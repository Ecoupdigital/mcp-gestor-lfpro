/**
 * Master registration entrypoint.
 *
 * Each tool module exports `register(): void` that calls
 * `registerTool({...})` from `../registry.js`.
 *
 * Tasks 4-7 will populate this with 40 tools (7+5+6+4+3+5+4+6).
 */
export function registerAllTools(): void {
  // populated in tasks 4-7
}
